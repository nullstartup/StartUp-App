android {
    ndkVersion rootProject.ext.ndkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion
    compileSdk rootProject.ext.compileSdkVersion

    namespace "com.startupapp"
    defaultConfig {
        applicationId "com.startupapp"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.0"
<<<<<<< HEAD
    }
    signingConfigs {
        debug {
            storeFile file("${projectDir}/startup.jks")
            storePassword '123456' 
            keyAlias 'startup-key-alias' 
            keyPassword '123456' 
=======

        // JNI için gerekli olan C++ destek dosyasını belirtin
        externalNativeBuild {
            cmake {
                cppFlags "-std=c++17"
            }
>>>>>>> 3c02766d4ac63b455afed918158bffa1bfec41f1
        }
    }
signingConfigs {
    debug {
        storeFile file("${projectDir}/startup.jks")
        storePassword '123456' 
        keyAlias 'startup-key-alias' 
        keyPassword '123456' 
    }
}


    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17 // Java 17 kullanımı
        targetCompatibility = JavaVersion.VERSION_17 // Java 17 kullanımı
    }

    // Java 17 yolu belirtmek için ekleyin
    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(17)
        }
    }

    signingConfigs {
        debug {
            storeFile file('C:/Users/hasan/Desktop/StartUp-App/android/app/startup.jks')
            storePassword '123456'
            keyAlias 'startup-key-alias'
            keyPassword '123456'
        }
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.debug
        }
        release {
            signingConfig signingConfigs.debug
            minifyEnabled false
            proguardFiles getDefaultProguardFile("proguard-android.txt"), "proguard-rules.pro"
        }
    }

    // CMake yapılandırmasını ekleyin
    externalNativeBuild {
        cmake {
            path "src/main/cpp/CMakeLists.txt" // CMake dosyasının yolunu belirtin
        }
    }
}

dependencies {
    implementation("com.facebook.react:react-android")
    if (hermesEnabled) {
        implementation("com.facebook.react:hermes-android")
    } else {
        implementation 'org.webkit:android-jsc:+' 
    }
}
